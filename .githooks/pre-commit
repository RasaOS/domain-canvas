#!/bin/sh
# Doctrine gate — a red check-doctrine blocks the commit.
# Enable once per clone: git config core.hooksPath .githooks
cd "$(git rev-parse --show-toplevel)" || exit 1
exec python3 bin/check-doctrine
